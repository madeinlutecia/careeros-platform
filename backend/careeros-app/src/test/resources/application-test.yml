spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

careeros:
  security:
    jwt:
      secret: test-secret-key-with-at-least-256-bits-for-hmac-sha256-algorithm
  ai:
    python-service-url: http://localhost:8000
